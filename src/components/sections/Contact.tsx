import FadeIn from '../ui/FadeIn'

export default function Contact() {
  return (
    <section id="contact" className="min-h-screen py-20 px-8 flex items-center justify-center">
      <FadeIn>
        <h2 className="text-3xl md:text-4xl font-bold text-white">Contacto (pendiente)</h2>
      </FadeIn>
    </section>
  )
}